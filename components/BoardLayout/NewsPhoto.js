import React from "react"
import PhotoLayout from "./PhotoLayout"

const NewsPhoto = ({ data }) => {
    return <PhotoLayout data={data} layoutWidth="200" layoutHeight="150" />
}

export default NewsPhoto
